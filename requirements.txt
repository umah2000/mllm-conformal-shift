# Core analysis dependencies (used by everything in analysis/ and calibration/).
# These run fine on CPU -- no GPU required for anything except data_generation/.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Only needed for scripts in data_generation/ (require a GPU-backed
# environment such as Google Colab or Kaggle Notebooks):
# torch>=2.2
# transformers>=4.44
# sentence-transformers>=3.0
# datasets>=2.20
# accelerate
# pillow
# qwen-vl-utils
